PORT=3000
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/task_tracker
JWT_SECRET=replace-with-a-long-random-secret

# Optional. If set, users can sign up as Admin only by providing this code.
# The first registered user is always allowed to become Admin.
ADMIN_SIGNUP_CODE=
